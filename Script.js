let feedback=[];
        
function addFeedback(){
    let str=document.getElementById("enfb").value;
    let i=feedback.length;           
    feedback[i]=str;
    let j=document.getElementById('fb').value=feedback;
}